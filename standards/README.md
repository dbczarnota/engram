---
type: meta
tags: [meta, standards-index]
status: active
date: 2026-05-25
---

# Standards

Cross-project rules — "how we always do X". Living documents (update in place).

- Create/update with `/remember-standard` (explicit only — never inferred).
- One file per topic, kebab-case (`standards/<topic>.md`). Use `_meta/templates/standard.md`.
- See `commit-messages.md` for the expected shape. Delete it once you have your own.
