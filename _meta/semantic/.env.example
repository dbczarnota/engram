# Semantic search (optional). Copy this file to `.env` and fill in your key.
#   cp .env.example .env
# The embedder reads GEMINI_API_KEY (or GOOGLE_API_KEY) from the environment.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
